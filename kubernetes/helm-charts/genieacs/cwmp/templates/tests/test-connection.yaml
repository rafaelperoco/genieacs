apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "cwmp.fullname" . }}-test-connection"
  labels:
    {{- include "cwmp.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "cwmp.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
