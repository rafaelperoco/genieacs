apiVersion: v1
kind: Service
metadata:
  name: {{ include "cwmp.fullname" . }}
  labels:
    {{- include "cwmp.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "cwmp.selectorLabels" . | nindent 4 }}
