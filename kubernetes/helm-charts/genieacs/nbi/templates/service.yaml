apiVersion: v1
kind: Service
metadata:
  name: {{ include "nbi.fullname" . }}
  labels:
    {{- include "nbi.labels" . | nindent 4 }}
spec:
  type: {{ .Values.service.type }}
  ports:
    - port: {{ .Values.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nbi.selectorLabels" . | nindent 4 }}
