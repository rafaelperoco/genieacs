apiVersion: v1
kind: Pod
metadata:
  name: "{{ include "fs.fullname" . }}-test-connection"
  labels:
    {{- include "fs.labels" . | nindent 4 }}
  annotations:
    "helm.sh/hook": test
spec:
  containers:
    - name: wget
      image: busybox
      command: ['wget']
      args: ['{{ include "fs.fullname" . }}:{{ .Values.service.port }}']
  restartPolicy: Never
